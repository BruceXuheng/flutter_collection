import 'package:flutter/material.dart';

class PlayCardPage extends StatelessWidget {
  const PlayCardPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}