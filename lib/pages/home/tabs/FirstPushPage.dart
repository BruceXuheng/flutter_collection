import 'package:flutter/material.dart';

class FirstPushPage extends StatelessWidget {
  const FirstPushPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}